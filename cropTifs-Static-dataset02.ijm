/*
 * author: Daniel Walther
 * creation date: <=19.06.2023 (d.m.yyyy) (`<=` because this file's code was overwritten to do the normalised cropping for dataset03, instead of it having been copied to a new file)
 * 
 * The original (i.e., actually used) crop...dataset02.ijm version was structured the same way as crop...dataset01.ijm, with the exception of different & individual cropping coordinates.
 */
